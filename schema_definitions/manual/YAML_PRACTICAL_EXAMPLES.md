# 📚 YAML 실전 예제 모음

> 실제로 사용 가능한 예제로 배우는 YAML 스키마 정의

---

## 목차

1. [초급 예제](#초급-예제)
2. [중급 예제](#중급-예제)
3. [고급 예제](#고급-예제)
4. [실전 프로젝트](#실전-프로젝트)

---

## 초급 예제

### 예제 1: 기본 섹션 규칙 만들기

**목표**: "Basic" 섹션과 "Advanced" 섹션으로 나누기

```yaml
# schema-logic.yaml

sectionRules:
  # 규칙 1: group이 명시된 경우
  - name: "Use Explicit Group"
    description: "x-ui.group이 있으면 그대로 사용"
    condition:
      type: "has-explicit-group"
    action:
      type: "use-explicit-group"
  
  # 규칙 2: 나머지는 Advanced
  - name: "Fallback to Advanced"
    description: "모든 다른 필드는 Advanced"
    condition:
      type: "always"
    action:
      type: "assign-section"
      section: "Advanced"

sectionOrder:
  - "Basic"
  - "Advanced"
```

**시각화:**

```
입력 스키마:
┌────────────────────────────────┐
│ FIELD_A:                        │
│   x-ui:                         │
│     group: "Basic"              │ ← 명시된 그룹
│                                 │
│ FIELD_B:                        │
│   x-ui:                         │
│     label: "Label B"            │ ← 그룹 없음
└────────────────────────────────┘

↓ (규칙 적용)

화면 출력:
┌────────────────────────────────┐
│ 📦 Basic                        │
│    └─ FIELD_A                  │
│                                 │
│ 📦 Advanced                     │
│    └─ FIELD_B                  │
└────────────────────────────────┘
```

---

### 예제 2: 타입별 섹션 나누기

**목표**: 각 TYPE 값에 따라 다른 섹션으로 분류

```yaml
# schema-logic.yaml

sectionRules:
  # 규칙 1: BEAM 타입
  - name: "Beam Section"
    condition:
      type: "visible-types-include-any"
      types: ["BEAM"]
    action:
      type: "assign-section"
      section: "Beam Properties"
  
  # 규칙 2: PLATE 타입
  - name: "Plate Section"
    condition:
      type: "visible-types-include-any"
      types: ["PLATE"]
    action:
      type: "assign-section"
      section: "Plate Properties"
  
  # 규칙 3: 나머지
  - name: "Common"
    condition:
      type: "always"
    action:
      type: "assign-section"
      section: "Common Properties"

sectionOrder:
  - "Common Properties"
  - "Beam Properties"
  - "Plate Properties"

typeLabels:
  BEAM: "Beam"
  PLATE: "Plate"
```

**시각화:**

```
입력 스키마:
┌────────────────────────────────────────┐
│ TYPE:                                   │
│   enum: ["BEAM", "PLATE"]               │
│                                         │
│ BEAM_OFFSET:                            │
│   x-ui:                                 │
│     visibleWhen:                        │
│       TYPE: ["BEAM"]                    │
│                                         │
│ PLATE_THICKNESS:                        │
│   x-ui:                                 │
│     visibleWhen:                        │
│       TYPE: ["PLATE"]                   │
│                                         │
│ MATL:                                   │
│   x-ui:                                 │
│     label: "Material"                   │
└────────────────────────────────────────┘

↓ (규칙 적용)

화면 출력 (TYPE = "BEAM" 선택 시):
┌────────────────────────────────────────┐
│ 📦 Common Properties                    │
│    ├─ TYPE                             │
│    └─ MATL                             │
│                                         │
│ 📦 Beam Properties                      │
│    └─ BEAM_OFFSET                      │
└────────────────────────────────────────┘

화면 출력 (TYPE = "PLATE" 선택 시):
┌────────────────────────────────────────┐
│ 📦 Common Properties                    │
│    ├─ TYPE                             │
│    └─ MATL                             │
│                                         │
│ 📦 Plate Properties                     │
│    └─ PLATE_THICKNESS                  │
└────────────────────────────────────────┘
```

---

## 중급 예제

### 예제 3: 복합 조건 사용하기

**목표**: 여러 조건을 조합하여 복잡한 섹션 분류

```yaml
# schema-logic.yaml

sectionRules:
  # 규칙 1: 명시적 그룹이 있으면 우선
  - name: "Explicit Group Priority"
    condition:
      type: "has-explicit-group"
    action:
      type: "use-explicit-group"
  
  # 규칙 2: TENSTR과 COMPTR 둘 다 포함
  - name: "Tension and Compression"
    condition:
      type: "visible-types-include-all"
      types: ["TENSTR", "COMPTR"]
    action:
      type: "assign-section"
      section: "Tension/Compression"
  
  # 규칙 3: TENSTR만
  - name: "Tension Only"
    condition:
      type: "visible-types-exact"
      types: ["TENSTR"]
    action:
      type: "assign-section"
      section: "Tension only"
  
  # 규칙 4: COMPTR만
  - name: "Compression Only"
    condition:
      type: "visible-types-exact"
      types: ["COMPTR"]
    action:
      type: "assign-section"
      section: "Compression only"
  
  # 규칙 5: WALL 타입
  - name: "Wall Section"
    condition:
      type: "visible-types-include-any"
      types: ["WALL"]
    action:
      type: "assign-section"
      section: "Wall"
  
  # 규칙 6: visibleWhen이 없으면 Common
  - name: "No Condition"
    condition:
      type: "no-visible-when"
    action:
      type: "assign-section"
      section: "Common Keys and Solid"
  
  # 규칙 7: 폴백
  - name: "Fallback"
    condition:
      type: "always"
    action:
      type: "assign-section"
      section: "Advanced"

sectionOrder:
  - "Common Keys and Solid"
  - "Tension only"
  - "Compression only"
  - "Tension/Compression"
  - "Wall"
  - "Advanced"

typeLabels:
  TENSTR: "Tension only"
  COMPTR: "Compression only"
  WALL: "Wall"
```

**결정 트리:**

```
                    ┌────────────────────┐
                    │   필드 입력         │
                    └─────────┬──────────┘
                              │
                              ↓
              ┌───────────────────────────┐
              │ has-explicit-group?       │
              └───────┬───────────────────┘
                      │
          ┌───────────┼──────────┐
          YES                    NO
          │                      │
          ↓                      ↓
    ┌──────────┐    ┌───────────────────────────┐
    │ 그룹 사용 │    │ visible-types-include-all │
    └──────────┘    │ ["TENSTR", "COMPTR"]?     │
                    └───────┬───────────────────┘
                            │
                ┌───────────┼──────────┐
                YES                    NO
                │                      │
                ↓                      ↓
          ┌─────────────┐    ┌──────────────────┐
          │ Tension/    │    │ visible-types-   │
          │ Compression │    │ exact ["TENSTR"]?│
          └─────────────┘    └────┬─────────────┘
                                  │
                      ┌───────────┼─────────┐
                      YES                   NO
                      │                     │
                      ↓                     ↓
              ┌──────────────┐    ┌────────────────┐
              │ Tension only │    │ (다음 규칙 확인)│
              └──────────────┘    └────────────────┘
```

---

### 예제 4: 검증 레이어 정의

**목표**: 표준 검증과 애플리케이션 검증을 분리

```yaml
# schema-logic.yaml

validationLayerRules:
  # 표준 JSON Schema 검증
  STD:
    description: "Standard JSON Schema validation"
    triggers:
      - has-enum
      - has-type
      - has-minItems
      - has-maxItems
      - has-minimum
      - has-maximum
      - has-pattern
  
  # 애플리케이션 레벨 검증
  APP:
    description: "Application-specific validation"
    triggers:
      - has-enumByType
      - has-nodeCountByType
      - has-valueConstraint
      - has-dependsOn
  
  # 비즈니스 로직 검증
  BIZ:
    description: "Business logic validation"
    triggers:
      - has-businessRule
      - has-crossFieldValidation
```

**검증 흐름:**

```
입력값
  │
  ↓
┌─────────────────────────────────────────┐
│ STD 레이어 (표준 검증)                   │
│ ━━━━━━━━━━━━━━━━━━━━                    │
│                                          │
│ ✓ 타입이 맞는가? (type)                  │
│ ✓ enum 값 중 하나인가?                   │
│ ✓ 최소/최대값 범위 안인가?               │
│ ✓ 패턴에 맞는가? (regex)                 │
│                                          │
└──────────────────┬──────────────────────┘
                   │ (통과)
                   ↓
┌─────────────────────────────────────────┐
│ APP 레이어 (애플리케이션 검증)           │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━━             │
│                                          │
│ ✓ TYPE별 enum이 맞는가?                  │
│ ✓ NODE 개수가 TYPE에 맞는가?             │
│ ✓ 값 제약 조건 만족? (양수/음수)         │
│ ✓ 의존 필드가 있는가?                    │
│                                          │
└──────────────────┬──────────────────────┘
                   │ (통과)
                   ↓
┌─────────────────────────────────────────┐
│ BIZ 레이어 (비즈니스 로직 검증)          │
│ ━━━━━━━━━━━━━━━━━━━━━━━━━              │
│                                          │
│ ✓ 비즈니스 규칙 만족?                    │
│ ✓ 다른 필드와의 관계가 올바른가?         │
│                                          │
└──────────────────┬──────────────────────┘
                   │ (통과)
                   ↓
              ✅ 검증 완료
```

---

## 고급 예제

### 예제 5: 동적 레이블 조합

**목표**: 여러 타입의 레이블을 자동으로 조합

```yaml
# schema-logic.yaml

sectionRules:
  # 여러 타입이 있을 때 레이블 조합
  - name: "Multiple Types Combined"
    condition:
      type: "visible-types-multiple"
      minCount: 2
    action:
      type: "join-type-labels"
      separator: " / "
  
  # 단일 타입
  - name: "Single Type"
    condition:
      type: "visible-types-multiple"
      minCount: 1
    action:
      type: "join-type-labels"
  
  # 폴백
  - name: "Fallback"
    condition:
      type: "always"
    action:
      type: "assign-section"
      section: "General"

typeLabels:
  BEAM: "Beam"
  TRUSS: "Truss"
  PLATE: "Plate"
  WALL: "Wall"
  SOLID: "Solid"
```

**결과 예시:**

```
입력 1:
  visibleWhen:
    TYPE: ["BEAM"]
결과: "Beam"

입력 2:
  visibleWhen:
    TYPE: ["BEAM", "TRUSS"]
결과: "Beam / Truss"

입력 3:
  visibleWhen:
    TYPE: ["BEAM", "TRUSS", "PLATE"]
결과: "Beam / Truss / Plate"

입력 4:
  visibleWhen 없음
결과: "General"
```

---

### 예제 6: 복잡한 조건 매칭

**목표**: includes, excludes, fields를 사용한 정밀한 매칭

```yaml
# schema-logic.yaml

sectionRules:
  # WALL 포함하지만 다른 특정 타입은 제외
  - name: "Wall Only (Strict)"
    condition:
      type: "visible-types-match"
      includes: ["WALL"]
      excludes: ["BEAM", "TRUSS", "PLATE"]
    action:
      type: "assign-section"
      section: "Wall Specific"
  
  # PLATE 관련이지만 STYPE 필드가 있어야 함
  - name: "Plate with Subtype"
    condition:
      type: "visible-types-match"
      includes: ["PLATE"]
      fields:
        - name: "STYPE"
          required: true
    action:
      type: "assign-section"
      section: "Plate Advanced"
  
  # 조건부 필수 필드
  - name: "Cable Properties"
    condition:
      type: "visible-types-match"
      includes: ["TENSTR"]
      fields:
        - name: "STYPE"
          equals: 3
    action:
      type: "assign-section"
      section: "Cable Specific"
```

**조건 평가 예시:**

```
예시 1: Wall Only (Strict)
┌────────────────────────────────────────┐
│ 필드:                                   │
│   visibleWhen:                          │
│     TYPE: ["WALL"]                      │
│                                         │
│ 평가:                                   │
│ ✓ includes ["WALL"]? YES                │
│ ✓ excludes 중 포함? NO                  │
│                                         │
│ 결과: "Wall Specific" ✓                 │
└────────────────────────────────────────┘

예시 2: Wall + Plate (제외됨)
┌────────────────────────────────────────┐
│ 필드:                                   │
│   visibleWhen:                          │
│     TYPE: ["WALL", "PLATE"]             │
│                                         │
│ 평가:                                   │
│ ✓ includes ["WALL"]? YES                │
│ ✗ excludes 중 포함? YES (PLATE)         │
│                                         │
│ 결과: 다음 규칙 확인 ✗                  │
└────────────────────────────────────────┘

예시 3: Cable Properties
┌────────────────────────────────────────┐
│ 필드: CABLE_LENGTH                      │
│   visibleWhen:                          │
│     TYPE: ["TENSTR"]                    │
│     STYPE: 3                            │
│                                         │
│ 평가:                                   │
│ ✓ includes ["TENSTR"]? YES              │
│ ✓ STYPE = 3? YES                        │
│                                         │
│ 결과: "Cable Specific" ✓                │
└────────────────────────────────────────┘
```

---

## 실전 프로젝트

### 프로젝트 1: 완전한 ELEM 스키마 로직

실제 운영 환경에서 사용 가능한 완전한 예제입니다.

```yaml
# schema-logic.yaml (완전판)

# ════════════════════════════════════════════════════════════
# ELEM API 스키마 로직 정의
# ════════════════════════════════════════════════════════════

# ┌─────────────────────────────────────────────────────────┐
# │ 1. 섹션 규칙 (우선순위 순)                               │
# └─────────────────────────────────────────────────────────┘

sectionRules:
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 우선순위 1: 명시적 그룹
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - name: "Explicit Group"
    description: "x-ui.group이 명시되어 있으면 그대로 사용"
    condition:
      type: "has-explicit-group"
    action:
      type: "use-explicit-group"
  
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 우선순위 2: 조건 없는 필드 (Common)
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - name: "No Visible When (Common)"
    description: "visibleWhen이 없으면 공통 필드"
    condition:
      type: "no-visible-when"
    action:
      type: "assign-section"
      section: "Common Keys and Solid"
  
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 우선순위 3: Tension/Compression 복합
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - name: "Tension and Compression"
    description: "TENSTR과 COMPTR 둘 다 포함"
    condition:
      type: "visible-types-include-all"
      types: ["TENSTR", "COMPTR"]
    action:
      type: "assign-section"
      section: "Tension/Compression"
  
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 우선순위 4: Tension only
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - name: "Tension Only"
    description: "TENSTR만 포함"
    condition:
      type: "visible-types-include-any"
      types: ["TENSTR"]
    action:
      type: "assign-section"
      section: "Tension only"
  
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 우선순위 5: Compression only
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - name: "Compression Only"
    description: "COMPTR만 포함"
    condition:
      type: "visible-types-include-any"
      types: ["COMPTR"]
    action:
      type: "assign-section"
      section: "Compression only"
  
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 우선순위 6: Wall
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - name: "Wall"
    description: "WALL 타입 포함"
    condition:
      type: "visible-types-include-any"
      types: ["WALL"]
    action:
      type: "assign-section"
      section: "Wall"
  
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 우선순위 7: Plate
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - name: "Plate"
    description: "PLATE, PLSTRS, PLSTRN 포함"
    condition:
      type: "visible-types-include-any"
      types: ["PLATE", "PLSTRS", "PLSTRN"]
    action:
      type: "assign-section"
      section: "Plate"
  
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 우선순위 8: Fallback (Advanced)
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  - name: "Fallback to Advanced"
    description: "기타 모든 필드"
    condition:
      type: "always"
    action:
      type: "assign-section"
      section: "Advanced"

# ┌─────────────────────────────────────────────────────────┐
# │ 2. 섹션 표시 순서                                        │
# └─────────────────────────────────────────────────────────┘

sectionOrder:
  - "Common Keys and Solid"
  - "Tension only"
  - "Compression only"
  - "Tension/Compression"
  - "Wall"
  - "Plate"
  - "Advanced"

# ┌─────────────────────────────────────────────────────────┐
# │ 3. 타입 레이블 (코드명 → 표시명)                         │
# └─────────────────────────────────────────────────────────┘

typeLabels:
  BEAM: "Beam"
  TRUSS: "Truss"
  TENSTR: "Tension only"
  COMPTR: "Compression only"
  PLATE: "Plate"
  WALL: "Wall"
  PLSTRS: "Plate Stress"
  PLSTRN: "Plate Strain"
  AXISYM: "Axisymmetric"
  SOLID: "Solid"

# ┌─────────────────────────────────────────────────────────┐
# │ 4. 검증 레이어 규칙                                      │
# └─────────────────────────────────────────────────────────┘

validationLayerRules:
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # STD: 표준 JSON Schema 검증
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  STD:
    description: "Standard JSON Schema validation"
    triggers:
      - has-enum          # enum 값 검증
      - has-type          # 타입 검증
      - has-minItems      # 배열 최소 항목 수
      - has-maxItems      # 배열 최대 항목 수
      - has-minimum       # 숫자 최소값
      - has-maximum       # 숫자 최대값
      - has-pattern       # 정규식 패턴
      - has-format        # 문자열 포맷
  
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # APP: 애플리케이션 레벨 검증
  # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  APP:
    description: "Application-specific validation"
    triggers:
      - has-enumByType           # TYPE별 다른 enum
      - has-nodeCountByType      # TYPE별 NODE 개수
      - has-valueConstraint      # 값 제약 조건 (양수/음수)
      - has-enumLabelsByType     # TYPE별 enum 레이블
      - has-visibleWhen          # 조건부 표시

# ┌─────────────────────────────────────────────────────────┐
# │ 5. 가시성 평가 규칙                                      │
# └─────────────────────────────────────────────────────────┘

visibilityRules:
  # TYPE 필드 값에 따른 표시 여부 결정
  - field: "TYPE"
    type: "enum-match"
    description: "TYPE 값이 visibleWhen.TYPE 배열에 포함되면 표시"
  
  # STYPE 필드 값에 따른 표시 여부 결정
  - field: "STYPE"
    type: "value-match"
    description: "STYPE 값이 visibleWhen.STYPE과 일치하면 표시"

# ════════════════════════════════════════════════════════════
# 설정 완료!
# ════════════════════════════════════════════════════════════
```

**테스트 케이스:**

```
┌───────────────────────────────────────────────────────────────┐
│ 테스트 1: TYPE 필드 (조건 없음)                                │
├───────────────────────────────────────────────────────────────┤
│ 입력:                                                          │
│   TYPE:                                                        │
│     type: string                                               │
│     enum: ["BEAM", "TRUSS", ...]                               │
│                                                                │
│ 규칙 매칭: "No Visible When (Common)"                          │
│ 결과: "Common Keys and Solid" ✓                                │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│ 테스트 2: TENS 필드 (TENSTR + COMPTR)                         │
├───────────────────────────────────────────────────────────────┤
│ 입력:                                                          │
│   TENS:                                                        │
│     x-ui:                                                      │
│       visibleWhen:                                             │
│         TYPE: ["TENSTR", "COMPTR"]                             │
│                                                                │
│ 규칙 매칭: "Tension and Compression"                           │
│ 결과: "Tension/Compression" ✓                                  │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│ 테스트 3: STYPE 필드 (TENSTR만)                                │
├───────────────────────────────────────────────────────────────┤
│ 입력:                                                          │
│   STYPE:                                                       │
│     x-ui:                                                      │
│       visibleWhen:                                             │
│         TYPE: ["TENSTR"]                                       │
│                                                                │
│ 규칙 매칭: "Tension Only"                                      │
│ 결과: "Tension only" ✓                                         │
└───────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────┐
│ 테스트 4: WALL 필드 (명시적 그룹)                              │
├───────────────────────────────────────────────────────────────┤
│ 입력:                                                          │
│   WALL:                                                        │
│     x-ui:                                                      │
│       group: "Wall Configuration"                              │
│       visibleWhen:                                             │
│         TYPE: ["WALL"]                                         │
│                                                                │
│ 규칙 매칭: "Explicit Group"                                    │
│ 결과: "Wall Configuration" ✓                                   │
└───────────────────────────────────────────────────────────────┘
```

---

## 실습 문제

### 문제 1: 기본 섹션 추가

**과제**: "Geometry" 섹션을 추가하고, `X`, `Y`, `Z` 필드를 이 섹션에 넣으세요.

```yaml
# 여기에 작성하세요
sectionRules:
  - name: "Geometry Section"
    # TODO: 조건 작성
    action:
      # TODO: 액션 작성
```

<details>
<summary>정답 보기</summary>

```yaml
sectionRules:
  - name: "Geometry Section"
    description: "Geometry 그룹의 필드들"
    condition:
      type: "has-explicit-group"
    action:
      type: "use-explicit-group"

# 스키마에서:
# X:
#   x-ui:
#     group: "Geometry"
# Y:
#   x-ui:
#     group: "Geometry"
# Z:
#   x-ui:
#     group: "Geometry"
```

</details>

---

### 문제 2: 조건부 섹션

**과제**: `MODE` 값이 "LINEAR"일 때만 "Linear Analysis" 섹션에 넣으세요.

```yaml
# 여기에 작성하세요
```

<details>
<summary>정답 보기</summary>

```yaml
sectionRules:
  - name: "Linear Analysis"
    description: "LINEAR 모드 전용 필드"
    condition:
      type: "visible-types-include-any"
      types: ["LINEAR"]
    action:
      type: "assign-section"
      section: "Linear Analysis"
```

</details>

---

### 문제 3: 검증 레이어 추가

**과제**: "CRITICAL" 레이어를 추가하여 `has-criticalField` 트리거가 있으면 적용하세요.

```yaml
# 여기에 작성하세요
validationLayerRules:
  # TODO
```

<details>
<summary>정답 보기</summary>

```yaml
validationLayerRules:
  STD:
    description: "Standard JSON Schema validation"
    triggers:
      - has-enum
      - has-type
  
  APP:
    description: "Application-specific validation"
    triggers:
      - has-enumByType
  
  CRITICAL:
    description: "Critical field validation"
    triggers:
      - has-criticalField
```

</details>

---

## 추가 자료

### 온라인 YAML 검증 도구

- [YAML Lint](http://www.yamllint.com/)
- [Code Beautify - YAML Validator](https://codebeautify.org/yaml-validator)
- [Online YAML Parser](https://yaml-online-parser.appspot.com/)

### 디버깅 팁

```javascript
// 브라우저 콘솔에서 현재 로드된 규칙 확인
import { getSchemaLogicRules } from '@/lib/schema/schemaLogicEngine';
console.log(getSchemaLogicRules());

// 특정 필드의 섹션 결정 과정 추적
import { determineSectionName } from '@/lib/schema/schemaLogicEngine';
const section = determineSectionName(yourField);
console.log('Determined section:', section);

// 캐시 초기화 (변경사항 즉시 반영)
import { clearSchemaLogicCache } from '@/lib/schema/schemaLogicEngine';
clearSchemaLogicCache();
```

---

**문서 버전**: 1.0  
**최종 업데이트**: 2026-01-14  
**작성자**: API Verification Team


