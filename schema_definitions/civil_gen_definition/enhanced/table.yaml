# Enhanced Schema - Table (Spec Tab) Specific Rules
# Enhanced Schema의 Spec Tab 테이블 렌더링 규칙

version: "1.0"
extends: "./ui-rules.yaml"

# ============================================================================
# Table Structure
# 테이블 구조
# ============================================================================
tableStructure:
  columns:
    - id: "no"
      header: "No."
      width: "6%"
      align: "center"
    
    - id: "description"
      header: "Description"
      width: "35%"
      align: "left"
    
    - id: "key"
      header: "Key"
      width: "14%"
      align: "left"
    
    - id: "type"
      header: "Value Type"
      width: "10%"
      align: "center"
    
    - id: "default"
      header: "Default"
      width: "10%"
      align: "center"
    
    - id: "required"
      header: "Required"
      width: "25%"
      align: "left"
  
  numbering:
    start: 1
    resetOnSection: false  # Enhanced는 연속 번호

# ============================================================================
# Section Headers (x-ui.group)
# 섹션 헤더 렌더링
# ============================================================================
sectionHeaders:
  enabled: true
  
  style:
    background: "bg-cyan-950/30"
    textColor: "text-cyan-400"
    fontSize: "text-xs"
    fontWeight: "font-semibold"
    padding: "p-2"
    icon: "📋"
  
  colspan: 6
  
  detectBy:
    - "section"
    - "__section_"

# ============================================================================
# Row Rendering
# 행 렌더링 규칙
# ============================================================================
rowRendering:
  # 일반 행
  standard:
    hover: true
    hoverStyle: "hover:bg-zinc-800/30"
  
  # Description 컬럼
  description:
    source: "x-ui.label"  # x-ui.label 사용
    fallback: "key"
    format: "label"
    markdown: true
    
    # Enhanced 특징 표시
    features:
      # Standard enum
      enum:
        enabled: true
        title: "**Enum Values:**"
        format: "• {value} - {label}"
        style: "bg-green-50 border-l-4 border-green-500"
      
      # x-enum-by-type (TYPE별 enum)
      enumByType:
        enabled: true
        title: "**Enum Values by Type:**"
        format: "*{type}:*\n• {value} - {label}"
        style: "bg-blue-50 border-l-4 border-blue-500"
      
      # x-value-constraint
      valueConstraint:
        enabled: true
        title: "**Value Constraints:**"
        format: "• {type}: {constraint}"
        style: "bg-amber-50 border-l-4 border-amber-500"
        badge: "APP"
      
      # x-node-count-by-type
      nodeCountByType:
        enabled: true
        title: "**Node Count by Type:**"
        format: "• {type}: {count} nodes"
        style: "bg-purple-50 border-l-4 border-purple-500"
        badge: "APP"
    
    # Validation badges
    validationBadges:
      standard:
        text: "STD"
        className: "text-[9px] px-1.5 py-0.5 rounded bg-blue-900/50 text-blue-300 border border-blue-700/50"
      appLayer:
        text: "APP"
        className: "text-[9px] px-1.5 py-0.5 rounded bg-orange-900/50 text-orange-300 border border-orange-700/50"
  
  # Key 컬럼
  key:
    format: "\"{key}\""
    style:
      color: "text-blue-400"
      fontFamily: "font-mono"
  
  # Type 컬럼
  type:
    format: "capitalize"
    arrayFormat: "Array[{itemType}]"
  
  # Default 컬럼
  default:
    emptyValue: "-"
    format: "raw"
  
  # Required 컬럼 (TYPE별 상이할 수 있음)
  required:
    dynamicByType: true  # TYPE별로 다른 required 상태
    
    # 모든 TYPE에 대해 같은 경우
    simple:
      required:
        text: "Required"
        className: "px-2 py-0.5 text-xs rounded bg-red-600/20 text-red-400"
      optional:
        text: "Optional"
        className: "px-2 py-0.5 text-xs rounded bg-zinc-700/50 text-zinc-400"
    
    # TYPE별로 다른 경우
    complex:
      format: |
        **Required:** {requiredTypes}
        **Optional:** {optionalTypes}
      style: "text-xs"

# ============================================================================
# Nested Fields (children)
# 중첩 필드 렌더링
# ============================================================================
nestedFields:
  enabled: true
  expandable: true
  expandIcon: "▼"
  collapseIcon: "▶"
  
  style:
    indentation: "pl-8"
    background: "bg-zinc-900/50"
    keyColor: "text-amber-400"
  
  # 중첩 필드 내 섹션 헤더
  nestedSectionHeader:
    enabled: true
    style:
      background: "bg-blue-950/30"
      textColor: "text-blue-400"

# ============================================================================
# Styling
# 테이블 스타일
# ============================================================================
styling:
  table:
    border: true
    borderColor: "border-zinc-800"
    borderCollapse: true
  
  header:
    background: "bg-zinc-900"
    textColor: "text-white"
    fontSize: "text-sm"
  
  body:
    alternateRows: false
    fontSize: "text-sm"
  
  cell:
    padding: "p-3"
    borderColor: "border-zinc-800"
  
  # Enhanced 특화 스타일
  featureBoxes:
    margin: "mt-2"
    padding: "p-2"
    borderRadius: "rounded"
    fontSize: "text-xs"

